spring:
  datasource:
    url: jdbc:mysql://localhost:3306/liv2train
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
        ddl-auto: update
    properties:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8181